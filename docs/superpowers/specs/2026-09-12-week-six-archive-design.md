# W6-M5 三调芭蕉扇·取经档案总编

2026-09-12 用户确认真实 Python 编排＋本地整理＋最终核验，授权后续本地细节自主决定，明确调用模型小队，要求交付本地 **One-level playable**。这是已批准设计和验收约定，不是运行证据；未授权 commit、push、merge、deploy、安装或真实玩家数据操作。

## 现场与目标

实际工作区 `/Users/macmini-zz/.codex/worktrees/5806/少儿编程学习网页`，分支 `codex/w4-m3-branch-structure`，开工 HEAD `e3abe2934dd5d7ab08b4e4e4dca6a118e1dbab58` 与实时远端一致，tracked 干净。保留原四项未跟踪审计、依赖和回归/视觉产物。Documents 默认 checkout 是旧入口。

M5 现有课程目标为“代码、数据与核验合一”，旧序列占位不构成正式能力。此次用同一作品串联四种已学能力，不引入新 Python 语法、不连接或训练在线 AI、不上传孩子输入。保持明亮故事书视觉、失败零惩罚、帮助不代答、原作品/装备/历史与恢复语义。

## 来源与玩家任务

只有有效 M4 formal-v3 可进入；同步校验当前前序作品与完整来源链。M1 真正运行过的三行记录、M2 分类、M3 说明书及其事件表或分步列表、M4 原句/证据/处置只读可查，不改写为 M5 的练习输入，也不把内部 provenance 字段暴露给孩子。

故事材料沿用已核实的第59至61回儿童转述及已批准 M1–M4 作品：一调得到假扇、火势更旺；二调取得真扇、随后被骗回，未完成本次通行；三调最终借得真扇、灭火通行。不能写成二调是假扇，或仅凭扇子真假断定是否通行。材料不足仅指本题有限材料，不能宣称全书没有记载。前序作品与原著转述分别标注来源。

### 1. 真实代码编排

可见编辑器中的三张字典记录经循环读取“第几调”和“经过”，实际调用 `record_attempt` 产生三行表。初始练习把第二参数误读为调次；明确这是本关练习副本，M1 正式代码原样保留。孩子可以修正字段、误删/恢复记录和代码；缺行、错序、固定值、错误字段、语法/安全错误均产生相应失败而非标准输出。复用既有受限同源 Pyodide Worker、AST/语义轨迹协议；每次以当前可见完整代码真实运行，不以 stdout 或隐藏正确表判成功。

### 2. 本地整理

结构化输入初始为空：任务范围（完整三调，而非只二调）、三调事实选取、仅按给定材料/不足不编造的限制、事件表或分步列表。两种格式同等有效且改变真实呈现。只从当前实际运行记录和孩子选择生成草稿，不补选、纠错或隐藏错误选择。遗漏事实、范围不符、限制矛盾、格式缺失均阻塞；选项顺序/数量不是通关依据。

### 3. 亲手核验与定稿

正常本地草稿保持忠实。另设明确标注“教师编写的核验练习，不是真实 AI 回答，也不是你的程序输出”的附加练习，不能偷偷篡改孩子草稿制造错误。三句分别为：

1. “一调得到假扇、火势更旺；三调最终借得真扇并灭火通行。”材料支持，证据须覆盖一调和三调，处置为保留原句。
2. “二调取得真扇，所以当次已经灭火通行。”与材料冲突，依据应包括二调未通行的直接记录/分类，可补充取得和被骗回事实，不能强制唯一充分证据组合；处置为有据修订二调结果。
3. “三次借扇的整个过程恰好用了三十分钟。”本页材料不足，引用公开有限材料核查范围，处置为暂不能确认，不能改成另一个时间或宣判必假。

判断、证据、处置均初始为空，使用键盘和触屏可操作控件。最终报告忠实呈现孩子选择的依据及处置文本，保留原句对照；不能从判定器注入正确处置。最终作品包含本关代码与实际事件表、本地说明书和实际输出、三句核验附件；不是用点击“完成”直接授予证明。

## 状态与持久化

逻辑顺序为代码待运行→代码有效→整理有效→核验有效→成功保存正式作品。每次只展示首个实际阻塞，错误仍保留可观察输出。修改代码使下游运行与核验失效；修改整理要求使其输出及核验失效；修改核验使最终成功失效。可保留孩子已输入的选择作为草稿，但必须重新运行受影响环节，不能套用陈旧成功。异步 Worker 迟到、取消、导航离开或来源变化不得覆盖新输入。

正式证明绑定当前有效 M4 来源、材料版本、代码、canonical/Worker轨迹、整理输入输出、核验输入输出、session/run/work。读档完整同步重算，严格拒绝未知字段、伪造来源、错位时间、伪造运行和旧占位完成。旧记录仅历史，不生成正式 M5 证明。草稿和失败、正式作品刷新/关闭重开/导入导出可恢复；多标签冲突按现有备份语义处理；损坏恢复保护原始材料。

实施细化：为保持入口硬预算，M5 存档仅保存规范判定结果以及完整来源、材料版本、代码、实际 Worker 轨迹/表和孩子选择；报告文案、表格/列表与核验附件由这些已验证数据按材料版本忠实重建，不另存一套可篡改的展示缓存。所有通关语义和来源绑定仍在读档时同步重算；不是将证明校验延后到界面加载。既有 M1–M4 作品格式不变。

只有成功持久化才正式完成；保存失败精确重试同一事务，不重复计次、奖励或提前永久通关。第30关是当前课程终点，不虚构 W7；地图、作品与家长摘要读取真实证明和尝试。只读重放不新增尝试、不覆盖前序作品。新增通关不等于整周成长体系/全站商业完成，不新增装饰性奖励体系。

## 可执行验收约定

- 默认代码真实产生重复调次并失败；修正字段产生三行正确表。缺记录、错序、读错字段、固定参数、非法代码不能绕过。真实 Worker 的 imports、文件/浏览器访问、超时、取消、加载失败和迟到消息边界维持。
- 本地输入逐项缺失/错误/矛盾均失败；两格式均通过且输出准确，不能点完所有项通过。输出数据源为本次实际运行结果。
- 三句独立注入错误判断、无关/缺少证据和错误处置均失败；充分证据合法组合均通过。输入变化后旧运行不允许完成；提示不改输入、不运行。
- 真实 M4→M5路径，以孩子可见编辑器/控件完成；前序两种 M3 输出真实保存显示。空白输入、历史/伪造来源、输入/run/work/proof 篡改不产生 formal-v3。
- 草稿/失败/成功刷新恢复，终点作品只读重放，连续保存故障精确重试，家长导入导出/恢复、多标签冲突、旧版本迁移、前序作品/装备不变。
- 五配置沿用 Chromium1440×1024、Firefox1440×1024、WebKit768×1024、Chromium390×844及320×844，workers=1、retries=0。包含键盘焦点、触屏、窄屏可读性/无横向溢出、减少动态、静音、必要素材失败恢复。只用隔离合成存档。
- 入口 gzip JS ≤184320 B、保守首页 ≤650 KiB，保持其余既有硬预算。开工入口184259 B余61 B；不得放宽阈值、削弱同步完整证明校验或掩盖历史 W1 advanced cold 超限。界面与执行器依既有合法边界延迟加载。
- 复用已登记火焰山生成素材并补本关槽位登记、实际视觉验收；不新增手绘代码插画、占位图或未经核实来源素材。
- 相关 unit、共享存档/schema/家长与受影响旧关回归、TypeScript、工程合同、素材/视觉、workflow、生产构建/预算和完整本关五配置浏览器门禁通过后，再由 root 对普通生产包独立验收。失败原始记录与修复映射保留，不把局部通过称为全站绿灯。

## 真实分工

- `/root/w6m5_implementation` 请求 `gpt-5.6-sol/high`：唯一产品功能链、测试、验收记录和 current-work 写入者。
- `/root/w6m5_budget` 请求 `gpt-5.6-terra/high`：只读依赖/预算分析。
- root：设计、批准记录、独立验收脚本/结果与普通生产浏览器验收。

上述为实际工具请求参数，不声称额外获取到服务端路由元数据；不再委派。实施与验证结果另见本关验收记录。

## 本地交付证据

2026-09-12 已按本规格完成 W6-M5 本地 One-level playable。最终普通生产构建的独立 11 组验收通过，58.242 秒，1010 个相关文件前后指纹一致；包括真实 M4→M5、失败保存与恢复、来源绑定、素材故障和完整键盘导航。五配置 W6-M5 浏览器 16/16、全量单元 2056/2056 通过。模型小队分工、首次失败与修复、各项证据范围及历史全站性能缺口见 [本关验收记录](../../verification/week-six-archive.md)。未提交、推送或部署。

# W4-M4 第三次变化：逐项观察册

用户于 2026-09-09 授权代理直接判断最优方案并完成本地实现和验收，不再选择玩法。沿用明亮儿童绘本方向。预选交付等级 One-level playable；W4-M5、周系统、全站和部署 not complete。

## 玩法与语义

孩子在可见 CodeMirror 修改一个 appearances 列表及 for 循环中的 print 参数。默认列表顺序为老妇、女子、老翁，循环错误地每次记录固定“老翁”。必须分别理解列表的先后顺序和循环变量 item；只修其中之一不能完成。公开原著参考说明第一次女子、第二次老妇、第三次老翁，列表是整理这些已知事实的输入，不是隐藏答案。实际运行先显示每轮 item 和记录值，再核对是否按原著顺序逐一且完整记录。允许空列表、缺项、重复和乱序成为可保存的学习失败，最多五项；print 可用 item 或三个外形中的固定字符串。只允许三行有限语法，任意导入、属性访问、文件、网络、while 和额外调用拒绝执行。草稿允许输入中的语法错误，失败零惩罚。

原著第27回三变均属于白骨精。核验完成后固定文字尾声说明悟空识破白骨精、唐僧误解并写贬书；不把打击或逐走当孩子命令，不以失败处罚悟空。W4-M5仍为后续总试炼。

## 完整因果链

可见草稿先保存 → 严格有限 grammar → 独立 Worker AST 验证 → 编译并真正执行 Python → list-created 与逐轮 callback trace → 与输入等价语义校验 → 保存 run → 原子保存作品/正式证明/任务完成 → 持久解锁 W4-M5、家长摘要。stdout 不决定成功。同步 grammar 只用于导入、存档和 Worker 一致性校验，不能替代运行。帮助只呈现已保存实际事实，不改代码、不执行或填写答案。

独立 W4-M4 session、work、proof；Progress revision 11。前置要求正式 W4-M3。历史完成保留时间和兼容访问，不伪造证据；真实重玩才能升级。正式回放重新运行 Worker 但不更改 session/作品/次数/完成时间。编辑、重跑清除旧观察。保护 W4-M1/M2/M3 和装备。保存失败停留在明确可重试状态；跨页 CAS 冲突提供备份和载入外部进度。

## 验收与预算

五项目沿用桌面 Chromium 1440x1024、平板 WebKit 768x1024、手机 Chromium 390x844、Firefox 1440x1024、窄屏 Chromium 320x844；键盘、触控、减弱动画、静音。workers=1 retries=0。W4-M4 lazy closure（含唯一 Worker）最多3MiB raw，同源 Pyodide 最多15MiB，冷结果20秒、热结果1秒；既有首页/全站失败不隐去。

回归必须包括双错误、分别单错、空/缺/重复、语法/安全拒绝、真实 Worker、重跑、刷新、导入导出、损坏保留、首次/草稿/run/观察/完成保存故障、跨标签冲突、历史升级、回放只读、家长/W4-M5解锁、五项目视图。复用现有批准白虎岭背景，以真实文本观察表呈现逐轮结果，不用绘制占位插画。

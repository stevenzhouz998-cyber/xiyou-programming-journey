# W4-M5 白虎岭核验站

2026-09-11 用户授权代理决定玩法，随后确认本方向。工作区5806，基线7720af2；最初授权本地实施与验证，2026-09-11进一步明确授权交接并提交、推送当前分支；未授权合并或部署。预选交付等级 One-level playable，现已达到；证据见 `docs/verification/week-four-verification-station.md`。

## 可见玩法与执行合同

延续第四周明亮绘本与白虎岭背景。四张始终可见的公开证据卡：女子、老妇、老翁三张原著变化卡身份为白骨精；另一张外形为老妇、身份为采药人的卡明确标注逻辑练习、非原著情节。故事资料沿用第四周已经核实的第二十七回。

两轮队列提前展示：[女子、老妇、老翁、练习] 与 [练习、老翁、女子、老妇]。一键完整运行同一份六行Python，两轮均使用公开 cards 列表。孩子编辑身份赋值源、比较符和两个分支动作。read_identity/read_appearance、keep_observing/polite_help 为本关提供的读取/记录工具，不引入函数定义。for、变量、if/else 综合使用，不需元组拆包或新语法。

默认读取外形，两个分支均继续核验；这是两个独立错误。相同外形的异身份卡阻止只看外形蒙对。每轮真实执行保留卡片、读入变量、条件结果和动作；报告第一处错误及发生轮次，不提前提供答案。身份读取错误优先，其次是分支动作错误；一轮正确不足以通关。所有失败零惩罚，原著被逐为固定文字回顾。

## 安全、保存与恢复

可见CodeMirror及快捷控件修改同一代码；未完成草稿可保存。512字纯文本 envelope 与六行有限grammar分离。独立Worker进行AST白名单检查，在空builtins环境真实执行，两轮最多八条事件；拒绝import、属性访问、任意调用、循环变形、无限循环。同步TS只复核真实trace与导入等价语义，不代替Worker执行。每次编辑清除陈旧结果；同代码重跑替换旧观察。

正式W4-M4为前置。Progress revision12新增session、w4-m5-verification-report作品和formal-v3证明。草稿先保存再运行，结果保存后展示，完成时原子封存作品/证明/任务并解锁W5-M1。家长摘要区分身份来源、分支与环境故障。旧版完成仅迁移legacy-replay-only，保留历史时间；正式重玩才升级。完成回放真实执行但不写原作品或次数。损坏导入拒绝，源备份及跨标签冲突沿用协调器。

## 实施顺序和验收

先写失败合同与语义检查，再实现合同/Worker、session/schema、协调器/路由/编辑器，最后真实浏览器验收。

必须覆盖：默认双错与分别单错、正确两轮、第二轮错误不能通过、稀疏/伪造/迟到trace、结构/安全拒绝、加载/超时/取消恢复；首次/草稿/运行/观察/完成保存失败、刷新、导出导入、旧版升级、损坏源保留、跨标签冲突、正式回放只读、家长摘要和W5-M1解锁。

五项目：桌面Chromium1440x1024、Firefox1440x1024、手机Chromium390x844、窄屏Chromium320x844、平板WebKit768x1024；workers1、retries0。键盘和触控同源输入、44px控件、静音/减弱动画。预算沿用W4-M4：closure含Worker≤3MiB，固定Pyodide≤15MiB，冷首次≤20秒、热运行≤1秒；Chromium10Mbps/4xCPU，其他引擎原生标注。复用批准背景，素材清单新增本关使用位置。

旧关作品与证明必须保持；公共存档回归及W4相关浏览器批量执行。全站旧失败仍为验收缺口，不从单关完成外推第四周系统闭环、全站或商业完成。

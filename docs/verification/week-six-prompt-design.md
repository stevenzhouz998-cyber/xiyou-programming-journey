# W6-M3 二调芭蕉扇任务说明书验收

日期：2026-09-12。状态：W6-M3 已达到本地 **One-level playable**。验收时未提交、推送、合并或部署，也没有操作真实玩家存档。用户随后明确授权本次 W6-M3 交接、commit and push；最终提交和远端状态见 Git 与工作区外交接文件。下列测试属于提交前相同产品输入证据，不冒充提交后重跑。

## 现场、授权与范围

工作区 `/Users/macmini-zz/.codex/worktrees/5806/少儿编程学习网页`，分支 `codex/w4-m3-branch-structure`，实施基线 HEAD `7f6fac0e0e6d964c3de21876f7ea4539cf224fe9`。用户确认核心玩法，授权本地细节自主决定，并要求模型小队实施到 One-level playable。规格：[已确认设计](../superpowers/specs/2026-09-12-week-six-prompt-design.md)。

本关是本地规则模拟：不调用在线 AI、不上传孩子内容、不宣称模型训练。孩子从空白状态选择任务、候选事实、限制条件和输出格式；选择真实驱动当前任务说明书与运行结果。事件表和分步列表均合法且产生不同结构。输出只使用已选事实，不暗中补答案；矛盾任务、错误身份、第三次才通行的污染、缺少关键事实或限制都会失败。一次只报告首个阻塞，提示只讲检查方向，失败不扣资源。

## 实际分工

- `/root/w6m3_implementation`：实际请求 Sol/high，产品功能链唯一写入者，负责源码、测试、素材清单、构建预算和本验收记录。
- `/root/w6m3_budget`：实际请求 Terra/high，只读分析入口依赖和可拆分边界，没有写产品文件。
- root：规格与授权记录、独立语义预期、迁移/证明/存档/浏览器验收及截图检查；没有与功能写入者并行修改同一产品链。

## 玩家结果与系统边界

- 有效 M2 `formal-v3` 作品以只读形式提供三次借扇分类和来源标识；补充的原著身份材料单独显示来源，不冒充 M2 已保存字段。候选事实卡不通过标签泄露正确项。
- 四个区域均由真实控件和保存会话驱动。缺项仍能运行局部输出，输出忠实保留所选的正确、错误或不完整陈述，再由首阻塞解释为何不能正式完成。
- 成功运行、session、作品和正式证明交叉绑定当前 M2 来源、输入、确定性运行、时间和作品标识。读档会重算并校验；旧 revision 19 的 M3 完成只迁移为历史兼容记录，不生成新 session、作品或 M4 解锁。
- 草稿、运行、提示和正式完成走共享协调保存；覆盖 session 与 completion 连续故障的原交易重试、多标签冲突、损坏快照恢复、素材未就绪后的挂起完成，以及刷新后继续封存。正式只读重放不改写存档。
- 表单保存后恢复真实键盘焦点；单选原生方向键和复选框 Tab/空格可连续操作。桌面、WebKit、移动触控、Firefox 与 320px 窄屏均纳入同一矩阵。
- 家长摘要只报告学习过程、失败次数和正式作品状态，不显示答案 ID、完整正确组合或内部来源字段。导出、清空、刷新和导入能恢复正式证明。
- 复用已有 `flaming-mountain-background.webp`，素材用途、来源、许可、尺寸和屏幕槽位已在素材清单登记；没有新增占位图或代码绘制插画。

## 独立普通生产验收

`artifacts/w6m3-independent/final-independent-result.json` 九组脚本全部通过，39.743 秒，输入、脚本、`public` 与普通生产 `dist` 前后指纹稳定；最终 manifest SHA-256 为 `c7044a0e7170d04b547ae5f344cd18ab6f4dd3e3d40ecf36b3bdb94b775d7139`。

- 独立语义 oracle 检查 2,304 组任务/事实子集/限制/格式组合、9 个非法输入和 6 种事实选择排列；两种输出结构不同且均不补选项。
- 独立 session、schema、revision 19→20 迁移和完整 progress 检查通过；浏览器保存后的正式证明另做 16 类篡改，均不能解锁 M4。
- 桌面与 320px 窄屏从真实 M2 正式作品进入 M3，再以 M3 正式证明解锁 M4；覆盖六种首阻塞、错误/局部输出、两种格式、提示不改输入、刷新只读、冲突和前序数据保留，无 `pageerror`。
- 原生连续 session/完成保存故障精确重试，`totalRuns` 保持 1；陈旧素材成功会在编辑后丢弃，刷新保留的有效成功在素材恢复后继续封存而不重跑。
- 键盘以 Space 选择后焦点跨保存保留，随后原生 ArrowDown/ArrowUp 和 Tab/Space 连续通过。修复前日志 `keyboard-dev.log`、`keyboard-fixed-dev.log` 与焦点调用取证保留，最终为 `final-keyboard-check.mjs.log`。
- 首页实际请求 21 项，工程口径 493744 B，本地解码 1084286 B；这是本地普通生产资源测量，不是线上传输或性能声明。

## 正式验证记录与失败闭合

所有目录以下均相对 `artifacts/verification/`；浏览器使用 `workers=1`、`retries=0`，合成存档仅存在于隔离浏览器上下文。

| 验证 | 结果 | 秒 | 证据目录或日志 |
| --- | --- | ---: | --- |
| W6-M3 五配置矩阵 | 15/15 | 54.708 | `1789194554720-988d8af0` |
| 旧关卡依赖拆分桌面聚焦回归 | 6/7，W1 历史冷启动门禁失败 | 34.359 | `1789194929852-ac7a77b7` |
| 全量单元，198 文件 | 2024/2024 | 98.450 | `artifacts/w6m3-local/unit-full-final.log` |
| TypeScript | 通过 | 4.958 | `1789194681909-eb5ac3a3` |
| 工程合同、普通构建、预算 | 247/247 及通过 | 11.364 | `1789194637794-2a94ff19` |
| 素材合同 | 60/60 | 1.751 | `1789194833942-8539b115` |
| 素材来源、槽位、尺寸与视觉门禁 | 通过 | 1.081 | `1789194669082-f65da7e9` |
| 证据工作流 | 5/5 | 0.582 | `1789194669083-435fc9ca` |

构建入口静态 JavaScript 为 184303/184320 B，余 17 B；保守首页 561.4/650 KiB。入口上限未放宽，完整同步读档和正式证明重校验没有移入异步逃避门禁。旧 W1–W3 的会话修改实现移至懒加载模块，并用已加载路由注册的初始化器保留同一事件内连续修改的原有同步语义；聚焦共享回归 95/95 通过，失败与关闭日志位于 `artifacts/w6m3-local/context-red.log` 和 `context-registry-fallback.log`。

保留并闭合的失败包括：严格解析器最初接受数组伪装 ID 和不可能日期；键盘保存时禁用 fieldset 导致焦点丢失；入口一度超预算；完整单元初轮修后仍有 7 个 revision 19 旧夹具/历史入口期望。最终独立 oracle、键盘、预算、聚焦 172/172 与完整 2024/2024 均通过，没有把失败结果计入最终绿灯。

旧关卡聚焦浏览器批次实际为 6/7，不记成全绿：W1 Dragon full、W2 Horse full/cold、W3 Manor full/cold 及共享多标签冲突/清空恢复通过；W1 cold 以所有响应原始字节计为3276172 B，超过2621440 B旧上限。用 HEAD `7f6fac0` 导出的隔离基线在同一构建、浏览器和用例下已为3253736 B，超限632296 B；本轮相对增长22436 B（0.69%）。证据分别是 `artifacts/verification/1789194929852-ac7a77b7/output.log` 与 `artifacts/w6m3-baseline-7f6fac0/cold-baseline-run.log`。阈值没有放宽，历史全站冷启动预算仍是残余风险，因此不声称全站回归或商业性能完成。

## 完成边界

本次只证明 W6-M3 在本地达到 One-level playable。它不代表第六周完整系统、三十关、全站或商业交付，也不包含线上部署、实体手机或真实玩家数据验证；移动触控证据来自浏览器设备模拟。M4 仍是后续关卡；当前本地预览必须携带有效 M2 正式作品才能进入可玩模式。入口只余 17 B，后续同步功能必须继续执行同一硬预算。

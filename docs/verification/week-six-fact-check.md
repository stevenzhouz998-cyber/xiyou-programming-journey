# W6-M4 核验而非猜测·回答审校台验收

日期：2026-09-12。状态：W6-M4 已达到本地 **One-level playable**。验收时未提交、推送、合并或部署，没有操作真实玩家存档。用户随后明确授权本次交接、commit and push；最终提交和远端见 Git 与工作区外交接文件。以下为提交前证据，不冒充提交后重新测试。

## 现场、授权与范围

工作区 `/Users/macmini-zz/.codex/worktrees/5806/少儿编程学习网页`，分支 `codex/w4-m3-branch-structure`，实施与验收基线 HEAD `50632834a98f3149c50bf5059543da93e61af4cb`。用户已确认核心玩法，授权后续细节自主决定，并要求使用模型小队交付至 One-level playable。规格：[已确认设计](../superpowers/specs/2026-09-12-week-six-fact-check-design.md)。

本关是本地确定性审校练习：不调用在线 AI，不上传孩子内容。只读展示有效 M3 正式作品，并将“人为编写的本地模拟回答”明确分开，不把预置错误冒充孩子上一关的输出。

## 实际分工

- `/root/w6m4_implementation`：实际请求 `gpt-5.6-sol/high`（未额外获取服务端运行路由元数据）；产品功能链唯一写入者，负责引擎、存档、UI、课程注册、测试、构建预算与本验收记录。
- `/root/w6m4_budget`：实际请求 `gpt-5.6-terra/high`（未额外获取服务端运行路由元数据）；只读分析同步校验边界和首屏预算，没有写产品文件。
- root：编写规格与授权记录，并独立验收语义组合、恶意存档、恢复、普通生产包、首页资源和截图。

## 玩家结果与系统边界

- 三句陈述的“判断、相关依据、处置”全部从空白开始。没有自动勾选、自动修订或隐藏答案；运行只报首个真实阻塞，失败为零惩罚，提示不修改输入。
- “二调取得真扇，后来又被骗回”需要两段直接转述覆盖前后事实，处置为保留原句。
- “取得真扇，所以当次已灭火通行”必须引用 M2 正式作品保留的“二调当次未通行”作为直接反证；两段取扇/被骗回转述可选作相关补充，共四种语义充分的有效子集。
- “恰好用了十分钟”只能判为“本页给出的材料不足以确认具体分钟数”，并显式选择暂不能确认。报告不声称原著没有时间描写，不制作伪造的“不存在”证据。
- 运行报告忠实保留每句原文、孩子选定的依据、判断和修订/暂缓处置。选择“保留原句”时使用当前句原文，不会被第一句替换。
- 成功的 session、run、作品和 `formal-v3` 证明交叉绑定 M3 来源、有限材料版本、全部输入、运行报告、时间和作品 ID。保存成功后才解锁 M5；历史兼容 M4 不生成 formal 证明，也不解锁 M5。
- 任何判断、证据或处置变化都会使旧成功失效。保存失败可精确重试，连续 session/完成保存故障不重跑已完成运行；跨标签冲突、损坏快照恢复、素材挂起后续存和导出/清空/导入均有真实浏览器证据。
- 桌面、Firefox、WebKit、390px 触控和 320px 窄屏共用同一产品路径；原生方向键、Tab/Space、焦点恢复、减少动效、静音和资源/懒加载失败边界均纳入配置或独立生产验收。
- 家长摘要只报告审校过程、运行次数和正式作品状态，不泄露答案 ID、完整正确组合或内部来源字段。
- 复用已登记的 `flaming-mountain-background.webp`，用途、来源、尺寸、许可和屏幕槽位已更新到素材清单；没有新增占位图或代码绘制插画。

## 独立普通生产验收

`artifacts/w6m4-independent/final-independent-result.json` 七组独立脚本全部通过，37.572 秒，`src`/`public`/普通生产 `dist`/脚本与配置前后指纹稳定。最终 manifest SHA-256 为 `818d8c4018ce02fa22212e8023c68107cbb5460fbc1c30b276d4216c584ef1f8`。

- 严格数据验证覆盖 M2–M4 非法日期/时区、prototype、symbol、多余字段、safe count 和 69 个历史 revision 边界。
- 语义 oracle 覆盖 3,840 组判断/证据/处置组合、9 个畸形输入、8 种来源删改、日期畸形、报告忠实性和陈旧 run/hint 失效。
- 桌面和 320px 均从真实 M3 重做的事件表/分步列表进入 M4，再以 M4 正式证明解锁 M5；覆盖空白状态、四类阻塞、错误报告不自修正、提示不改输入、键盘、草稿刷新、正式只读和前序数据保留，无 `pageerror`。
- 16 类正式存档篡改全部被拒绝；旧 M4 历史完成不解锁 M5。原生双保存故障、素材陈旧成功失效、刷新后素材恢复续存均通过。
- 首页实际请求 17 项，工程口径 493120 B/650 KiB，本地解码 1095062 B；这是本地普通生产资源测量，不是线上性能声明。
- `artifacts/w6m4-independent/visual-review-result.json` 目视检查通过：来源/练习归属、判断/证据/处置层次和原文/修订标签可读，桌面与 320px 卡片无水平溢出。

## 正式验证记录与失败闭合

浏览器工程记录使用 `workers=1`、`retries=0`，合成存档只存在于隔离浏览器上下文。

| 验证 | 结果 | 秒 | 证据目录或日志 |
| --- | --- | ---: | --- |
| W6 四关首批联合矩阵 | 62/78；16 个纯测试断言失败后闭合 | 287.585 | `1789199781550-1d59a8cc` |
| M1/M2 旧 revision 断言受影响复跑 | 16/21；仅 M4 五配置定位器失败 | 158.657 | `1789200257862-f9665a39` |
| M4 full 五配置定位器修正 | 5/5 | 31.579 | `1789200451687-9071115e` |
| 旧周代表路径（W1 full、W2/W3 full+cold、W4/W5 full+冲突/恢复） | 38/44；6 个旧 revision 断言失败后闭合 | 223.656 | `1789200505460-6add1ba0` |
| W4 corrupt + W5 full 断言修正 | 6/6 | 68.130 | `1789200762011-6bfb7ede` |
| 全量单元，202 文件 | 2039/2039 | 95.870 | `artifacts/w6m4-local/npm-test-final-2.log` |
| 工程合同 | 249/249 | 7.172 | `artifacts/w6m4-local/bundle-script-after-e2e-fixes.log` |
| 素材合同 | 60/60 | 1.668 | `artifacts/w6m4-local/npm-test-final-2.log` |
| TypeScript | 通过 | 5.457 | `artifacts/w6m4-local/typecheck-final.log` |
| 素材来源、槽位、尺寸与视觉门禁 | 通过 | 1.433 | `artifacts/w6m4-local/assets-visual-final.log` |
| 证据工作流 | 5/5 | 0.354 | `artifacts/w6m4-local/npm-test-final-2.log` |
| 普通生产构建与预算/循环门禁 | 通过 | 4.314 | `artifacts/w6m4-local/build-course-fix-final.log` 及 `budget-course-fix-final.log` |

M4 的有效合并覆盖为 **15/15**：桌面 Chromium 8 条、Firefox 2 条、WebKit 1 条、390px Chromium 2 条、320px Chromium 2 条。首批 78 条中的 16 个失败不计为最终绿灯：M1/M2 的三处当前存档版本期望由 19 改为 21，M4 初始空白检查改用唯一 radio 可访问名；受影响路径随后全部通过。

构建入口静态 JavaScript 为 **184259/184320 B**，余 61 B；保守首页 563.4/650 KiB。上限未放宽，同步读档和完整证明重校验未被移入异步包。已静态到达的 battle/engine/blockly/progress 模块在不改变闭包的前提下共用 `progress-core`；素材路径仍为 `asset-path`，焦点与懒加载边界仅合并为 `ui-shared`。manifest 循环、纯素材场景引入 progress/context/write 和动态路由静态泄漏门禁均保持绿色。

旧周代表批次中 W1 full、W2/W3 full+cold、W4/W5 Python Worker、多标签冲突和损坏恢复已闭合通过。W1 advanced cold 单独重验仍保留已知历史失败：Firefox/WebKit 2/2 通过，三个 Chromium 配置的 W1-M5 冷启动均为 3375416 B，超过旧 3145728 B 上限；证据 `1789200841123-9472f9a5`。为核实归因，从本次基线 HEAD `50632834a98f3149c50bf5059543da93e61af4cb` 隔离导出并以同一 desktop Chromium、同一 `@advanced-cold`、同一 3145728 B 上限重跑，基线已为 3361598 B 并失败；当前同配置为 3375416 B，增量 13818 B（0.41%）。基线日志位于 `artifacts/baseline-advanced-cold-chromium.log`，导出现场位于 `artifacts/w6m4-baseline-5063283/`。阈值未放宽，本次没有新建该失败，但增量和原有超限都保留为全站冷启动风险。

## 完成边界

本次只证明 W6-M4 在本地达到 One-level playable。它不代表第六周完整系统、三十关、全站或商业交付，也不包含线上部署、实体手机或真实玩家数据验证；移动触控证据来自浏览器设备模拟。本地预览需要有效 M3 `formal-v3` 作品才能正式进入 M4，只有成功保存的 M4 `formal-v3` 才解锁 M5。

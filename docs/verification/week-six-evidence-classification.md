# W6-M2 真假扇证据分类验收

日期：2026-09-12。状态：W6-M2 已达到本地 **One-level playable**。最终证据与历史失败分开记录；验收时尚未提交、推送或部署；用户随后授权本次交接、commit and push，最终状态见Git及工作区外交接文件。

## 现场与授权

工作区 `/Users/macmini-zz/.codex/worktrees/5806/少儿编程学习网页`，分支 `codex/w4-m3-branch-structure`，实施基线 `5cd99e920cc19851dccf9a7958a86933e62fa570`。用户确认核心玩法，授权本地细节自主决定，要求模型小队与 One-level playable。实施阶段不含 commit、push、merge 或部署；随后用户另行明确授权本次 commit and push，仍不含 merge 或部署。原有四项未跟踪内容保留；所有浏览器使用隔离合成存档。

规格：[已确认设计](../superpowers/specs/2026-09-12-week-six-evidence-classification-design.md)。本关为本地数据标注实验，不调用在线 AI、不宣称模型训练；不改写原著。输入是三条记录的双维度标签、分别选择的依据与材料不足判断，分组来自孩子输入。有效 M1 正式作品作为只读来源。

## 实际分工

- `/root/w6m2_implementation`：工具请求 Sol/high，产品功能链唯一写入者，负责源码、测试、五配置与构建预算。
- `/root/w6m2_budget_analysis`：工具请求 Terra/high，只读预算分析，完成后结束。
- root：规格与授权记录、共享素材槽位及合同、独立教学语义预期、存档篡改与普通浏览器验收。独占 `scripts/check-asset-manifest.mjs`、对应测试与素材清单；不与 Sol 同时写同一功能文件。

这里记录真实工具委派与请求参数，不将其称为另行收集的服务端运行元数据。

## 开发阶段独立检查

以下是本轮开发检查，有各自输入指纹或日志；后续修改需按相关性复验，最终结果另列。

- `artifacts/w6m2-independent/semantic-check.mjs`：独立于实现答案表建立教学预期；2,916 组标签/练习组合、117,649 组证据组合、6 种记录排列及 15 个非法输入/来源，检查是否成功、真实分组、输入不被改写与零惩罚。
- `schema-check.mjs`：19 个独立 session/证明反例，包含标签/依据/分组/时间/来源/计数、缺前置/作品/session，以及陈旧成功运行。检查提示不修改输入。
- `progress-check.mjs`：12 个完整 Progress 篡改拒绝，revision 18→19 保留全部前序作品、证明、装备；旧 M2 完成迁移为历史记录且不解锁 M3；正式完成幂等。
- `chain-dev.log`：桌面与 320px 窄屏从 M1 可见 Python 编辑和真实运行进入 M2，依次经历空白、错误标签、错误依据、材料误猜与正确完成，草稿刷新、只读重播、前序保留与 M3 解锁均通过；耗时 10.048/9.659 秒，无 pageerror。这是开发服务器证据。
- `recovery-dev.log`：原生 Storage 故障注入，核验 session 保存失败→重试 session 成功但正式作品保存失败→第二次重试完成；核验次数保持1，恢复期间核验和侧栏提示禁用。5.669 秒。
- `asset-stale-dev.log` 保留失败：素材未加载时成功核验后修改答案，随后恢复素材，旧 pending completion 使页面进入错误保存锁。修正清除陈旧成功并在封存前重算后，`asset-stale-fixed.log` 按原步骤通过，3.899 秒。
- 素材合同 60/60，1.721 秒；`assets.log` 与 `asset-gate.log`。新增 M2 槽位、错误图片路径和缺槽位拒绝。复用原图，无新增插画；桌面与窄屏已选依据现有可换行正文，修复完成状态下原生 select 截断不可读问题。
- `budget-probe.json` 与 `build-probe.log`：独立隔离构建入口静态 JS gzip 为187579 B，超过184320 B上限3259 B；这是一份保留的失败预算证据，不能作为最终达标数据。

## 最终门禁

预算已通过：普通构建入口184055/184320 B（余265 B），保守首页559.0/650 KiB。未放宽上限或削弱同步证明校验；新M2反馈文案移至懒加载内容，既有关卡的工厂及操作按实际依赖拆分。相关第四周、第五周回归在联合矩阵中覆盖。

普通生产包独立复验：`artifacts/w6m2-independent/final-independent-result.json` 八组脚本全部通过，39.406秒；脚本及生产manifest执行前后相同，manifest SHA-256 `04c7e5896e67dc79381ce89380ba3316c328ff1f3259c5966c10eab51a87ac71`。首页21项实际请求，工程口径493517 B（JavaScript gzip，其余原始体积），本地解码1086672 B；不是线上传输或性能改善声明。后续若有产品改动，须重新判断证据有效性。

新增中间状态恢复独立检查：成功核验但未取得正式作品时刷新，清楚显示“正式作品尚未保存”；素材恢复后封存原成功，核验次数仍为1。`asset-reload-dev.log` 保留开发证据；同一场景已纳入上述普通生产复验。

修复后的普通生产包已完成独立复验，日志 `artifacts/w6m2-independent/final-production-acceptance.log`。键盘修复前的独立结果保留为 `pre-keyboard-independent-result.json`，不作为最终包证据。独立最终脚本 `artifacts/w6m2-independent/final-independent.mjs` 包括语义、schema、完整存档、普通生产通关链、原生连续保存故障、素材恢复陈旧成功、首页实际请求资源和画面；记录脚本与生产 manifest 指纹。五配置联合矩阵另行保留。

## 正式验证记录与失败闭合

所有目录以下均相对 `artifacts/verification/`；时长为记录器墙钟时间，浏览器 workers=1、retries=0。

| 验证 | 结果 | 秒 | 证据目录 |
| --- | --- | ---: | --- |
| W6-M2 五配置完整矩阵 | 22/22 | 103.112 | `1789185261736-d35eb006` |
| W5-M1 Firefox 完整路径补验 | 1/1 | 33.443 | `1789185370106-9de63186` |
| 全量单元，194文件 | 2009/2009 | 93.820 | `1789185564519-f65e016b` |
| TypeScript | 通过 | 4.928 | `1789185673101-576d51d8` |
| 素材合同 | 60/60 | 1.849 | `1789185678212-1f103afe` |
| 工作流合同 | 5/5 | 0.536 | `1789185680245-8e137fee` |
| 素材来源/槽位/视觉门禁 | 通过 | 1.030 | `1789185680962-6699e31f` |
| 工程合同、普通构建、预算 | 245/245及通过 | 11.542 | `1789185682176-ffc70907` |

root 在最终文档整理前调用 `checkEvidence`，上述单元至构建六条记录均匹配当前指纹。浏览器通过后只调整 `src/progress/weekFourVariableSessionSchema.test.ts` 两处迁移期望18→19，产品与浏览器用例未变；因此按相关输入复用浏览器证据，不声称其保守全仓指纹仍能自动复用。

联合矩阵 `1789183589723-c012ec71` 实际为133通过、5失败，约18.9分钟；绝不记为一次138全绿。它覆盖 W4-M1～M5、W5-M1～M3 的 full/cold/storage 与 M2。原报告保留在 `artifacts/w6m2-matrix-failed-test-results/` 和 `artifacts/w6m2-matrix-failed-playwright-report/`。其中115条旧关卡成功证据未受随后仅M2键盘产品修复影响；另1条W5-M1失败已补验通过，M2全部22条按新产品包重跑通过，共覆盖138个原定项目。

失败处理如下：

- 第一轮在旧测试期望14/17而产品迁移19时停止；`1789183297320-83a8a21a/output.log`保留。另一次启动因本轮遗留4173端口在零测试时拒绝，`1789183563832-8cfd061f`保留；只停止本代理启动的遗留进程后重开。
- 键盘不能只归为脚本误用。事件取证显示原生select收到方向键但没有input/change；在本关为无修饰键的上下方向键提供明确处理，仍走原有受控选择和保存链，保留原生标签及Alt/Ctrl/Meta快捷键。Chromium与Firefox真实按键补验均通过；未用selectOption代替键盘证明。原始诊断 `artifacts/w6m2-keyboard-events-debug*.log` 保留。
- 连续保存故障测试原来识别了尚未替换的旧重试按钮；现在先等待核验次数落盘，再验证新正式保存恢复动作。产品连续故障修复此前已由root独立验证，最终再次通过。
- 家长测试必须按privacy存档状态等待确认出现、确认保存、解除inert后才输入一次PIN。导入真实成功后会按导入凭据重新锁定，随后用原PIN重入核对摘要。两个家长路径均补验通过，没有用重复补填掩盖问题。
- 全量单元首轮2007/2009，两个失败同为旧revision18期望，原日志 `1789185436684-09fb28a7`保留。仅更新期望后完整复跑2009/2009。

最终玩家结果：三行双维度输入、独立证据及材料不足判断；按孩子输入分组；首阻塞反馈与零惩罚；草稿刷新、准确保存重试、多页冲突与损坏恢复；真实正式只读重播；家长导出—清空—导入；保留前序作品/证明/装备，仅本关有效正式证明解锁M3。浏览器检查使用隔离合成存档，不操作真实用户数据。

## 完成边界

本次目标只限 W6-M2 本地 One-level playable；不代表第六周系统闭环、三十关、全站或商业上线。无线上部署或真实玩家数据验证。本关强制门禁已通过。首页入口仅余265 B，后续课程仍须执行原预算。预览在本机5199端口；新浏览器仍需有效的 M1 正式作品才能进入，不向用户浏览器注入合成进度。

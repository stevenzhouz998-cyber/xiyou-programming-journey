# W6-M5 三调芭蕉扇·取经档案总编验收

日期：2026-09-12。状态：W6-M5 已达到本地 **One-level playable**，并成为现有 30 关课程的正式终点。验收期间未提交、推送、合并或部署，没有操作真实玩家存档。

## 现场、授权与范围

工作区 `/Users/macmini-zz/.codex/worktrees/5806/少儿编程学习网页`，分支 `codex/w4-m3-branch-structure`，实施基线 HEAD `e3abe2934dd5d7ab08b4e4e4dca6a118e1dbab58`。用户确认真实 Python、本地结构化说明书和最终证据审校三段玩法，授权其余细节自主决定，并要求使用模型小队交付。规格：[已确认设计](../superpowers/specs/2026-09-12-week-six-archive-design.md)。

本关只在有效 W6-M4 `formal-v3` 作品存在时开放；M1–M4 的真实作品以只读方式呈现。处理全在本机完成，不调用在线 AI，也不上传孩子的代码或选择。

## 实际分工

- `/root/w6m5_implementation`（任务 `01a094d9-b93a-7e50-8551-f3169cf93e85`）：请求参数与宿主可见元数据均为 `gpt-5.6-sol/high`；完整产品功能链唯一写入者，负责引擎、Worker 接线、存档、UI、课程终点、测试、构建预算、素材清单和本验收记录。
- `/root/w6m5_budget`（任务 `01a094d9-e390-76a0-afe7-69b7572ef840`）：请求参数与宿主可见元数据均为 `gpt-5.6-terra/high`；只读分析入口体积与同步证明边界，没有写产品文件。宿主元数据证据保存在 `artifacts/w6m5-independent/model-squad-host-metadata.json`。
- root：编写规格与授权记录，并独立建立语义、严格 schema、进度篡改、普通生产浏览器、存储/素材恢复、键盘和首页终点验收。

## 玩家结果与系统边界

- 玩家能看到并修改一份包含三张字典和一个循环的 Python 程序。默认循环读取错误字段，受限同源 Pyodide Worker 会真实产生三行错误记录；页面只展示这次实际输出，不自动改正或补齐。
- 正确运行后，玩家亲自选择完整三调范围、三条事实、只按材料且不足时不编造的限制，以及事件表或分步列表。事实和限制使用集合语义，选择顺序不改变正确性；报告行序仍来自当前实际 Worker 输出。
- 三句判断练习明确标为“教师编写”，不冒充真实 AI、孩子输入或程序输出。支持、冲突、材料不足各覆盖一句；判断、依据、处置均从空白开始，错误处置原样呈现在报告中。
- 当前代码、当前 Worker trace/rows、M4 来源摘要、完整结构化选择、首个阻塞、失败快照和零惩罚结果同步重算并交叉绑定。来源、代码或输入变化会使依赖结果失效；只有成功保存当前全链结果、作品和 `formal-v3` 证明才完成。
- 最近一次真实失败会保存在草稿中；非法语法不会携带旧 Worker 证据。提示只定位检查区域，不编辑、不运行，也不改变答案或运行次数。
- session 保存、正式完成保存、损坏快照恢复、多标签冲突、导出/清空/导入、素材失败/挂起/陈旧消息、Worker 路由离开和懒加载重试均已覆盖。保存故障恢复不重复已完成的真实运行。
- 保存期间表单会短暂锁定；键盘焦点在保存完成后回到原控件，Space、Tab、Shift+Tab 和方向键可连续操作。桌面、Firefox、WebKit、390px 与 320px 使用同一路径，窄屏无水平溢出。
- 成功后没有虚构的 W7 或下一关。成功框不显示“继续下一关”，首页入口改为“回看取经档案”，家长页显示终点作品与正式证明，但不泄露内部答案 ID。
- 复用已批准并登记的 `flaming-mountain-background.webp`；未新增占位图、代码绘制插画或装饰性奖励系统，既有装备、能力、作品和前序证明保持不变。

## 独立普通生产验收

`artifacts/w6m5-independent/final-independent-result.json` 的最终 11 组全部通过，58.242 秒；受控的 1010 个源码、配置、脚本、普通 `dist` 和独立验收文件前后哈希一致。

- `source-check`、`semantic-check`、`session-check`、`schema-check` 与 `progress-check` 覆盖 M4 来源绑定、3,840 组判断/依据/处置组合、乱序选择、失败结果往返、12 类严格草稿边界、24 类正式存档篡改和合法 M4 变化导致的摘要失效。
- 普通生产浏览器从真实 M4 作品进入 M5，桌面和 320px 均经历默认错误输出、非法导入刷新、修复代码、整理阻塞、错误处置、正确完成、终点只读回放和首页回看入口；六次运行都来自实际 Worker，无 `pageerror`。
- 原生双保存故障精确恢复 session 与完成事务；额外路径确认草稿保存失败重试后，编辑器重新可写、代码可再次保存并真实运行。
- 素材 error、pending 与陈旧成功不会提前生成证明；素材恢复后可用既有成功运行完成。完整键盘路径覆盖 radio Space、Tab 到 checkbox、Space、Shift+Tab 和 ArrowDown。
- 桌面与 320px 截图由 root 实际查看，来源、教师练习归属、报告内容均可读，没有水平溢出。首页工程口径与入口预算也在普通生产包上独立复核通过。

早期普通浏览器预检曾出现一次“本次运行已完成，但结果尚未保存”，当次没有留下可归因的保存错误；随后同一完整路径、完整 schema 往返、正式五配置 E2E，以及最终独立双保存/恢复后编辑路径均稳定通过。该单次现象按事实保留，不把未证实原因写成产品结论。

## 正式验证记录与失败闭合

浏览器工程记录固定 `workers=1`、`retries=0`，合成存档只存在于隔离浏览器上下文。

| 验证 | 结果 | 秒 | 证据目录或日志 |
| --- | --- | ---: | --- |
| W6 M1–M5 首批联合五配置 | 84/94；M1–M4 为 78/78，M5 十项失败后闭合 | 402.156 | `1789208233881-6de72aa0` |
| M5 首次修正复跑 | 11/16；剩余五项为提示/Worker等待与恢复完成等待不足 | 76.001 | `1789208888232-6afbcd42` |
| M5 最终五配置 | 16/16 | 107.451 | `1789209095466-d4b5f1cb` |
| W4 损坏恢复 + W5 完整路径邻接回归 | 6/6 | 69.435 | `1789209266751-05dc4e7a` |
| 独立普通生产 11 组 | 11/11 | 58.242 | `artifacts/w6m5-independent/final-independent-result.json` |
| 全量单元、工程合同、素材合同与证据工作流 | 2056/2056、252/252、60/60、5/5 | 105.732 | `1789209395098-1ca382d6` |
| TypeScript | 通过 | 5.158 | `1789209539705-1d464d81` |
| 素材来源、槽位、尺寸与视觉门禁 | 通过 | 1.107 | `1789209539705-806942c4` |
| 普通生产构建 | 通过 | 3.949 | `1789209230561-657d7fd6` |
| 预算、循环、Worker 与静态隔离门禁 | 通过 | 0.577 | `1789209234695-f902a6c4` |

首批 M5 的十项失败没有计入绿灯：五个 full 是同文案的三个复选框被测试当作唯一元素；两个键盘失败揭示了保存锁定后焦点落到页面的真实缺陷；其余三项分别是恢复后编辑、损坏快照和懒加载测试越过保存完成或使用了不精确文案。焦点恢复按既有 M3/M4 可靠模式修复；测试等待可交互/已持久化信号后只输入一次。第二批剩余五项是提示保存或首次 Pyodide 结果未等到持久化就读取，最终 16/16 和独立普通包均通过。

最终普通构建入口静态 JavaScript 为 **184203/184320 B**，余 **117 B**；保守首页 **565.4/650 KiB**。M5 普通与重试路由闭包均为 **1198.7 KiB raw / 335.2 KiB gzip**，低于 3 MiB，并包含真实 `weekSixRecordsPython.worker`。预算未放宽，同步 schema/证明重算未转成异步；课程展示资料从入口闭包移到原本延迟加载的课程块，已经静态到达的共享模块只在同一入口闭包内重新分块。manifest 循环、纯素材场景隔离和路由静态泄漏门禁保持绿色。

## 完成边界

本次证明 W6-M5 在本地达到 One-level playable，并正确收束现有 30 关正式课程路径；不代表全站商业发布、线上部署、实体手机或真实玩家数据验证。移动证据来自浏览器设备模拟。W1 advanced cold 的历史 Chromium 超限仍按 W6-M4 验收记录保留：基线已失败，本次未放宽阈值，也未把局部门禁绿灯表述为全站性能完成。

普通生产预览由 root 保留在 `http://127.0.0.1:5207/xiyou-programming-journey/` 并已用隔离合成存档打开；端口状态会漂移，不是部署证据。
